import styled from "styled-components";

export const Container = styled.footer`
  padding: 3.9rem 11.6rem;

  background-color: ${({ theme }) => theme.COLORS.PURPLE_DARK};
  color: ${({ theme }) => theme.COLORS.WHITE};

  text-align: center;
`;
